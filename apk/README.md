# APK downloads

These are debug prototype APKs built from this repository.

- `gateway-debug.apk`: install on the gateway Android phone.
- `gateway-test-client-debug.apk`: install on the client Android phone.

SHA-256 values are recorded in `../artifacts/apk-sha256.txt` in the local build workspace and in the GitHub Release assets.
